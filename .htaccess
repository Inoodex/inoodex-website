# Laravel Root .htaccess - Redirect all requests to public folder

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Ignore requests that already point to public
    RewriteCond %{REQUEST_URI} !^/public/
    
    # Redirect everything else to public folder
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
